import { FC } from "react";

const Logo: FC = () => {
  return (
    <div className="logo flex h-full items-center justify-start text-xl">
      Subscription Manager
    </div>
  );
};

export default Logo;
